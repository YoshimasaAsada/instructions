# Project Rules

<!-- ============================================================
  このファイルは汎用テンプレートです。
  cp ~/develop/instructions/base-CLAUDE.md ./CLAUDE.md でプロジェクトルートに配置してください。
  「プロジェクト固有ルール」セクションを編集して、プロジェクトごとのルールを追加できます。
============================================================ -->

## スキルシステム

以下のスキル（指示書）が `~/develop/instructions/` に格納されている。
タスクの内容に応じて、最も適切なスキルを **自動で判断** し、実行前にファイルを読み込んで従うこと。

### スキルの読み込み方法

1. ユーザーのリクエストを受けたら、下記カタログからマッチするスキルを特定する
2. `cat ~/develop/instructions/{path}` でスキルファイルを読み込む
3. スキルの指示に従ってタスクを実行する
4. 複数のスキルが該当する場合は、最も関連性の高いものを選択する（必要なら組み合わせる）

### スキルカタログ

#### Claude Code 固有スキル（`claude/`）

| スキル | パス | トリガー例 |
|--------|------|-----------|
| フィーチャープランニング | `claude/create-docs.md` | 「〇〇を実装したい」「要件を整理して」「設計をまとめて」 |
| レビュー指摘反映 | `claude/fix-from-review.md` | 「レビュー指摘を反映して」「review.md を反映して」 |

#### 汎用スキル（`utils/`）

| スキル | パス | トリガー例 |
|--------|------|-----------|
| コードレビュー | `utils/review.md` | 「レビューして」「コードを見て」 |
| レビューループ | `utils/review-loop.md` | 「指摘がなくなるまでレビューして」 |
| レビュー修正 | `utils/fix-from-review.md` | 「レビュー指摘を修正して」（Claude固有機能不要の場合） |
| フィーチャープランニング | `utils/create-docs.md` | 「要件と設計をまとめて」（Claude固有機能不要の場合） |
| リファクタリング | `utils/refactor.md` | 「リファクタして」「整理して」 |
| テスト生成 | `utils/generate-tests.md` | 「テストを書いて」「テストケースを作って」 |
| デバッグ | `utils/debug.md` | 「バグを調べて」「エラーの原因を特定して」 |
| 型レビュー | `utils/type-review.md` | 「型を見直して」「any を潰して」 |
| スキャフォールド生成 | `spec-to-code/scaffold-from-spec.md` | 「spec.md から実装して」「スキャフォールドを作って」 |
| コミット整理 | `utils/clean-commits.md` | 「コミットを整理して」「squash して」 |
| PR作成 | `utils/create-pr.md` | 「PRを作って」「PR説明文を書いて」 |
| 影響範囲分析 | `utils/impact-analysis.md` | 「影響範囲を調べて」「依存関係を確認して」 |
| マイグレーション安全性 | `utils/migration-safety.md` | 「マイグレーションを確認して」「ロールバック計画を作って」 |
| ADR作成 | `utils/create-adr.md` | 「ADRを書いて」「技術選定を記録して」 |
| オンボーディングガイド | `utils/create-onboarding.md` | 「オンボーディング資料を作って」 |

### スキル選択の優先ルール

- Claude Code の機能（Explore エージェント、並列調査など）を活用できる場合 → `claude/` 配下を優先
- シンプルなタスクや Claude 固有機能が不要な場合 → `utils/` 配下を使用
- `claude/` と `utils/` に同名スキルがある場合、Claude Code 上で実行しているなら `claude/` を優先
- `utils/review.md` を使うレビューでは、関連するテストコードが存在する場合にテストケース不足も確認し、特に境界値・異常系・エッジケースの漏れを必ず指摘する

---

## プロジェクト固有ルール

<!-- ここにプロジェクト固有のルールを追記してください -->
<!-- 例:
- コーディング規約: TypeScript strict mode を使用
- テスト: vitest を使用、カバレッジ 80% 以上
- コミット: Conventional Commits に従う
- ブランチ: feature/ から develop へ PR を出す
-->
