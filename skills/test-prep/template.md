# testdata.md テンプレート

次の構成を基本とし、該当しない項目は理由つきで `N/A` とする。

````markdown
# 動作確認セット: <機能名>

## 概要
<何を確認するための準備か、入力にした文書、準備した範囲を3行程度で>

- 入力文書: <パス>
- 作成日時: <YYYY-MM-DD HH:MM>
- 対象ブランチ: <ブランチ名>

## 検証環境

| 項目 | 内容 | 根拠 |
|---|---|---|
| 接続先DB | <DB名 / ホスト> | <確認したファイル・コマンド> |
| 起動コマンド | `<コマンド>` | <justfile / compose.yaml 等のパス> |
| ベースURL | `<http://...>` | <ホスト定義のパス> |
| 停止コマンド | `<コマンド>` | |

<起動後に応答を確認した結果を1行で>

## データの印

<このセットで作成した全レコードに付けた識別子の規約。後片付けと再実行時の重複回避はこれを根拠にする>

- 名称プレフィックス: `[tp-<slug>] `
- メール: `test+tp-<slug>-NNN@example.com`

## 作成データ一覧

| # | 対象 | ID | 識別子 | 内容・状態 | 対応ケース |
|---|---|---|---|---|---|
| 1 | <テーブル / モデル> | <実ID> | <印> | <どういう状態のデータか> | TC-001 |

### ログイン情報

| 用途 | メール | パスワード | ロール | 備考 |
|---|---|---|---|---|
| <権限Aの確認用> | | | | |

## 動作確認URL

| # | 確認内容 | URL | ログイン | 到達確認 | 合格の見え方 | 対応ケース |
|---|---|---|---|---|---|---|
| 1 | <何を見るか> | `<プレースホルダを含まない完全なURL>` | <不要 / 上表のどのユーザーか> | 200 | <ここがこう表示されていれば合格> | TC-001 |

<URLが存在しない対象（バッチ・スキーマ変更等）は、代わりに実行コマンドと確認用クエリを書く>

## 確認順序

<上のURLを実施順に並べた checkbox リスト。状態遷移など順序に依存するものは依存関係を明記する>

- [ ] 1. <確認内容>（#1）

## 後片付け

<印に一致するレコードだけを削除する手順。対象件数を数えるコマンドを先に置き、削除は後に置く>

```bash
# 対象件数の確認（先にこれで件数を見る）
<コマンド>

# 削除
<コマンド>
```

## 再実行

<同じセットをもう一度用意する場合の手順。印による重複回避が効くため、原則そのまま再実行できることを明記する>

## 気づいた問題

<準備の過程で見つかった不具合・仕様の疑問。到達しなかったURL、投入できなかったデータとその原因。修正はこのスキルの責務外なので、事実だけを記録して implement / test-case-doc へ引き継ぐ>
````
